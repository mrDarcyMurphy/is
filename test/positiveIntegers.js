var assert = require('assert')
var is = require('../lib/is')
var fn = function(){}

describe('positive integers', function(){

  it('positiveInteger', function(){
    assert.equal(false, is.positiveInteger(fn))
    assert.equal(false, is.positiveInteger(new Date()))
    assert.equal(false, is.positiveInteger(undefined))
    assert.equal(false, is.positiveInteger(NaN))
    assert.equal(false, is.positiveInteger(null))
    assert.equal(false, is.positiveInteger(true))
    assert.equal(false, is.positiveInteger(false))
    assert.equal(false, is.positiveInteger({}))
    assert.equal(false, is.positiveInteger([]))
    assert.equal(false, is.positiveInteger(""))
    assert.equal(false, is.positiveInteger("  "))
    assert.equal(false, is.positiveInteger("asdf"))
    assert.equal(false, is.positiveInteger("1.23"))
    assert.equal(false, is.positiveInteger("-42"))
    assert.equal(false, is.positiveInteger("-42.01"))
    assert.equal(false, is.positiveInteger("-1"))
    assert.equal(false, is.positiveInteger("0"))
    assert.equal(false, is.positiveInteger("666"))
    assert.equal(false, is.positiveInteger(1.23))
    assert.equal(false, is.positiveInteger(-42))
    assert.equal(false, is.positiveInteger(-1))
    assert.equal(false, is.positiveInteger(0))
    assert.equal(true,  is.positiveInteger(666))
  })

  it('not.positiveInteger', function(){
    assert.equal(true , is.not.positiveInteger(fn))
    assert.equal(true , is.not.positiveInteger(new Date()))
    assert.equal(true , is.not.positiveInteger(undefined))
    assert.equal(true , is.not.positiveInteger(NaN))
    assert.equal(true , is.not.positiveInteger(null))
    assert.equal(true , is.not.positiveInteger(true))
    assert.equal(true , is.not.positiveInteger(false))
    assert.equal(true , is.not.positiveInteger({}))
    assert.equal(true , is.not.positiveInteger([]))
    assert.equal(true , is.not.positiveInteger(""))
    assert.equal(true , is.not.positiveInteger("  "))
    assert.equal(true , is.not.positiveInteger("asdf"))
    assert.equal(true , is.not.positiveInteger("1.23"))
    assert.equal(true , is.not.positiveInteger("-42"))
    assert.equal(true , is.not.positiveInteger("-42.01"))
    assert.equal(true , is.not.positiveInteger("-1"))
    assert.equal(true , is.not.positiveInteger("0"))
    assert.equal(true , is.not.positiveInteger("666"))
    assert.equal(true , is.not.positiveInteger(1.23))
    assert.equal(true , is.not.positiveInteger(-42))
    assert.equal(true , is.not.positiveInteger(-1))
    assert.equal(true , is.not.positiveInteger(0))
    assert.equal(false, is.not.positiveInteger(666))
  })

  it('posInt', function(){
    assert.equal(false, is.posInt(fn))
    assert.equal(false, is.posInt(new Date()))
    assert.equal(false, is.posInt(undefined))
    assert.equal(false, is.posInt(NaN))
    assert.equal(false, is.posInt(null))
    assert.equal(false, is.posInt(true))
    assert.equal(false, is.posInt(false))
    assert.equal(false, is.posInt({}))
    assert.equal(false, is.posInt([]))
    assert.equal(false, is.posInt(""))
    assert.equal(false, is.posInt("   "))
    assert.equal(false, is.posInt("asdf"))
    assert.equal(false, is.posInt("1.23"))
    assert.equal(false, is.posInt("-42"))
    assert.equal(false, is.posInt("-42.01"))
    assert.equal(false, is.posInt("-1"))
    assert.equal(false, is.posInt("0"))
    assert.equal(false, is.posInt("666"))
    assert.equal(false, is.posInt(1.23))
    assert.equal(false, is.posInt(-42))
    assert.equal(false, is.posInt(-1))
    assert.equal(false, is.posInt(0))
    assert.equal(true,  is.posInt(666))
  })

  it('not.posInt', function(){
    assert.equal(true , is.not.posInt(fn))
    assert.equal(true , is.not.posInt(new Date()))
    assert.equal(true , is.not.posInt(undefined))
    assert.equal(true , is.not.posInt(NaN))
    assert.equal(true , is.not.posInt(null))
    assert.equal(true , is.not.posInt(true))
    assert.equal(true , is.not.posInt(false))
    assert.equal(true , is.not.posInt({}))
    assert.equal(true , is.not.posInt([]))
    assert.equal(true , is.not.posInt(""))
    assert.equal(true , is.not.posInt("  "))
    assert.equal(true , is.not.posInt("asdf"))
    assert.equal(true , is.not.posInt("1.23"))
    assert.equal(true , is.not.posInt("-42"))
    assert.equal(true , is.not.posInt("-42.01"))
    assert.equal(true , is.not.posInt("-1"))
    assert.equal(true , is.not.posInt("0"))
    assert.equal(true , is.not.posInt("666"))
    assert.equal(true , is.not.posInt(1.23))
    assert.equal(true , is.not.posInt(-42))
    assert.equal(true , is.not.posInt(-1))
    assert.equal(true , is.not.posInt(0))
    assert.equal(false, is.not.posInt(666))
  })

  it('aPositiveInteger', function(){
    assert.equal(false, is.aPositiveInteger(fn))
    assert.equal(false, is.aPositiveInteger(new Date()))
    assert.equal(false, is.aPositiveInteger(undefined))
    assert.equal(false, is.aPositiveInteger(NaN))
    assert.equal(false, is.aPositiveInteger(null))
    assert.equal(false, is.aPositiveInteger(true))
    assert.equal(false, is.aPositiveInteger(false))
    assert.equal(false, is.aPositiveInteger({}))
    assert.equal(false, is.aPositiveInteger([]))
    assert.equal(false, is.aPositiveInteger(""))
    assert.equal(false, is.aPositiveInteger("    "))
    assert.equal(false, is.aPositiveInteger("asdf"))
    assert.equal(false, is.aPositiveInteger("1.23"))
    assert.equal(false, is.aPositiveInteger("-42"))
    assert.equal(false, is.aPositiveInteger("-42.01"))
    assert.equal(false, is.aPositiveInteger("-1"))
    assert.equal(false, is.aPositiveInteger("0"))
    assert.equal(true,  is.aPositiveInteger("666"))
    assert.equal(false, is.aPositiveInteger(1.23))
    assert.equal(false, is.aPositiveInteger(-42))
    assert.equal(false, is.aPositiveInteger(-1))
    assert.equal(false, is.aPositiveInteger(0))
    assert.equal(true,  is.aPositiveInteger(666))
  })

  it('not.aPositiveInteger', function(){
    assert.equal(true , is.not.aPositiveInteger(fn))
    assert.equal(true , is.not.aPositiveInteger(new Date()))
    assert.equal(true , is.not.aPositiveInteger(undefined))
    assert.equal(true , is.not.aPositiveInteger(NaN))
    assert.equal(true , is.not.aPositiveInteger(null))
    assert.equal(true , is.not.aPositiveInteger(true))
    assert.equal(true , is.not.aPositiveInteger(false))
    assert.equal(true , is.not.aPositiveInteger({}))
    assert.equal(true , is.not.aPositiveInteger([]))
    assert.equal(true , is.not.aPositiveInteger(""))
    assert.equal(true , is.not.aPositiveInteger("    "))
    assert.equal(true , is.not.aPositiveInteger("asdf"))
    assert.equal(true , is.not.aPositiveInteger("1.23"))
    assert.equal(true , is.not.aPositiveInteger("-42"))
    assert.equal(true , is.not.aPositiveInteger("-42.01"))
    assert.equal(true , is.not.aPositiveInteger("-1"))
    assert.equal(true , is.not.aPositiveInteger("0"))
    assert.equal(false, is.not.aPositiveInteger("666"))
    assert.equal(true , is.not.aPositiveInteger(1.23))
    assert.equal(true , is.not.aPositiveInteger(-42))
    assert.equal(true , is.not.aPositiveInteger(-1))
    assert.equal(true , is.not.aPositiveInteger(0))
    assert.equal(false, is.not.aPositiveInteger(666))
  })

  it('aPosInt', function(){
    assert.equal(false, is.aPosInt(fn))
    assert.equal(false, is.aPosInt(new Date()))
    assert.equal(false, is.aPosInt(undefined))
    assert.equal(false, is.aPosInt(NaN))
    assert.equal(false, is.aPosInt(null))
    assert.equal(false, is.aPosInt(true))
    assert.equal(false, is.aPosInt(false))
    assert.equal(false, is.aPosInt({}))
    assert.equal(false, is.aPosInt([]))
    assert.equal(false, is.aPosInt(""))
    assert.equal(false, is.aPosInt("    "))
    assert.equal(false, is.aPosInt("asdf"))
    assert.equal(false, is.aPosInt("1.23"))
    assert.equal(false, is.aPosInt("-42"))
    assert.equal(false, is.aPosInt("-42.01"))
    assert.equal(false, is.aPosInt("-1"))
    assert.equal(false, is.aPosInt("0"))
    assert.equal(true,  is.aPosInt("666"))
    assert.equal(false, is.aPosInt(1.23))
    assert.equal(false, is.aPosInt(-42))
    assert.equal(false, is.aPosInt(-1))
    assert.equal(false, is.aPosInt(0))
    assert.equal(true,  is.aPosInt(666))
  })

  it('not.aPosInt', function(){
    assert.equal(true , is.not.aPosInt(fn))
    assert.equal(true , is.not.aPosInt(new Date()))
    assert.equal(true , is.not.aPosInt(undefined))
    assert.equal(true , is.not.aPosInt(NaN))
    assert.equal(true , is.not.aPosInt(null))
    assert.equal(true , is.not.aPosInt(true))
    assert.equal(true , is.not.aPosInt(false))
    assert.equal(true , is.not.aPosInt({}))
    assert.equal(true , is.not.aPosInt([]))
    assert.equal(true , is.not.aPosInt(""))
    assert.equal(true , is.not.aPosInt("    "))
    assert.equal(true , is.not.aPosInt("asdf"))
    assert.equal(true , is.not.aPosInt("1.23"))
    assert.equal(true , is.not.aPosInt("-42"))
    assert.equal(true , is.not.aPosInt("-42.01"))
    assert.equal(true , is.not.aPosInt("-1"))
    assert.equal(true , is.not.aPosInt("0"))
    assert.equal(false, is.not.aPosInt("666"))
    assert.equal(true , is.not.aPosInt(1.23))
    assert.equal(true , is.not.aPosInt(-42))
    assert.equal(true , is.not.aPosInt(-1))
    assert.equal(true , is.not.aPosInt(0))
    assert.equal(false, is.not.aPosInt(666))
  })

  it('positiveInteger + arg', function(){
    assert.equal(false, is.positiveInteger(fn, 2))
    assert.equal(false, is.positiveInteger(new Date(), 2))
    assert.equal(false, is.positiveInteger(undefined, 2))
    assert.equal(false, is.positiveInteger(NaN, 2))
    assert.equal(false, is.positiveInteger(null, 2))
    assert.equal(false, is.positiveInteger(true, 2))
    assert.equal(false, is.positiveInteger(false, 2))
    assert.equal(false, is.positiveInteger({}, 2))
    assert.equal(false, is.positiveInteger([], 2))
    assert.equal(false, is.positiveInteger("", 2))
    assert.equal(false, is.positiveInteger("   ", 2))
    assert.equal(false, is.positiveInteger("asdf", 2))
    assert.equal(false, is.positiveInteger("1.23", 2))
    assert.equal(false, is.positiveInteger("-42", 2))
    assert.equal(false, is.positiveInteger("-42.01", 2))
    assert.equal(false, is.positiveInteger("-1", 2))
    assert.equal(false, is.positiveInteger("0", 2))
    assert.equal(false, is.positiveInteger("666", 2))
    assert.equal(false, is.positiveInteger(1.23, 2))
    assert.equal(false, is.positiveInteger(-42, 2))
    assert.equal(false, is.positiveInteger(-1, 2))
    assert.equal(false, is.positiveInteger(0, 2))
    assert.equal(false, is.positiveInteger(1, 2))
    assert.equal(true , is.positiveInteger(666, 2))

    assert.equal(false, is.positiveInteger(fn, -2))
    assert.equal(false, is.positiveInteger(new Date(), -2))
    assert.equal(false, is.positiveInteger(undefined, -2))
    assert.equal(false, is.positiveInteger(NaN, -2))
    assert.equal(false, is.positiveInteger(null, -2))
    assert.equal(false, is.positiveInteger(true, -2))
    assert.equal(false, is.positiveInteger(false, -2))
    assert.equal(false, is.positiveInteger({}, -2))
    assert.equal(false, is.positiveInteger([], -2))
    assert.equal(false, is.positiveInteger("", -2))
    assert.equal(false, is.positiveInteger("   ", -2))
    assert.equal(false, is.positiveInteger("asdf", -2))
    assert.equal(false, is.positiveInteger("1.23", -2))
    assert.equal(false, is.positiveInteger("-42", -2))
    assert.equal(false, is.positiveInteger("-42.01", -2))
    assert.equal(false, is.positiveInteger("-1", -2))
    assert.equal(false, is.positiveInteger("0", -2))
    assert.equal(false, is.positiveInteger("666", -2))
    assert.equal(false, is.positiveInteger(1.23, -2))
    assert.equal(false, is.positiveInteger(-42, -2))
    assert.equal(false, is.positiveInteger(-1, -2))
    assert.equal(false, is.positiveInteger(0, -2))
    assert.equal(true , is.positiveInteger(1, -2))
    assert.equal(true , is.positiveInteger(666, -2))
  })

  it('not.positiveInteger + arg', function(){
    assert.equal(true , is.not.positiveInteger(fn, 2))
    assert.equal(true , is.not.positiveInteger(new Date(), 2))
    assert.equal(true , is.not.positiveInteger(undefined, 2))
    assert.equal(true , is.not.positiveInteger(NaN, 2))
    assert.equal(true , is.not.positiveInteger(null, 2))
    assert.equal(true , is.not.positiveInteger(true, 2))
    assert.equal(true , is.not.positiveInteger(false, 2))
    assert.equal(true , is.not.positiveInteger({}, 2))
    assert.equal(true , is.not.positiveInteger([], 2))
    assert.equal(true , is.not.positiveInteger("", 2))
    assert.equal(true , is.not.positiveInteger("   ", 2))
    assert.equal(true , is.not.positiveInteger("asdf", 2))
    assert.equal(true , is.not.positiveInteger("1.23", 2))
    assert.equal(true , is.not.positiveInteger("-42", 2))
    assert.equal(true , is.not.positiveInteger("-42.01", 2))
    assert.equal(true , is.not.positiveInteger("-1", 2))
    assert.equal(true , is.not.positiveInteger("0", 2))
    assert.equal(true , is.not.positiveInteger("666", 2))
    assert.equal(true , is.not.positiveInteger(1.23, 2))
    assert.equal(true , is.not.positiveInteger(-42, 2))
    assert.equal(true , is.not.positiveInteger(-1, 2))
    assert.equal(true , is.not.positiveInteger(0, 2))
    assert.equal(true , is.not.positiveInteger(1, 2))
    assert.equal(false, is.not.positiveInteger(666, 2))

    assert.equal(true , is.not.positiveInteger(fn, -2))
    assert.equal(true , is.not.positiveInteger(new Date(), -2))
    assert.equal(true , is.not.positiveInteger(undefined, -2))
    assert.equal(true , is.not.positiveInteger(NaN, -2))
    assert.equal(true , is.not.positiveInteger(null, -2))
    assert.equal(true , is.not.positiveInteger(true, -2))
    assert.equal(true , is.not.positiveInteger(false, -2))
    assert.equal(true , is.not.positiveInteger({}, -2))
    assert.equal(true , is.not.positiveInteger([], -2))
    assert.equal(true , is.not.positiveInteger("", -2))
    assert.equal(true , is.not.positiveInteger("   ", -2))
    assert.equal(true , is.not.positiveInteger("asdf", -2))
    assert.equal(true , is.not.positiveInteger("1.23", -2))
    assert.equal(true , is.not.positiveInteger("-42", -2))
    assert.equal(true , is.not.positiveInteger("-42.01", -2))
    assert.equal(true , is.not.positiveInteger("-1", -2))
    assert.equal(true , is.not.positiveInteger("0", -2))
    assert.equal(true , is.not.positiveInteger("666", -2))
    assert.equal(true , is.not.positiveInteger(1.23, -2))
    assert.equal(true , is.not.positiveInteger(-42, -2))
    assert.equal(true , is.not.positiveInteger(-1, -2))
    assert.equal(true , is.not.positiveInteger(0, -2))
    assert.equal(false, is.not.positiveInteger(1, -2))
    assert.equal(false, is.not.positiveInteger(666, -2))
  })

  it('posInt + arg', function(){
    assert.equal(false, is.posInt(fn, 2))
    assert.equal(false, is.posInt(new Date(), 2))
    assert.equal(false, is.posInt(undefined, 2))
    assert.equal(false, is.posInt(NaN, 2))
    assert.equal(false, is.posInt(null, 2))
    assert.equal(false, is.posInt(true, 2))
    assert.equal(false, is.posInt(false, 2))
    assert.equal(false, is.posInt({}, 2))
    assert.equal(false, is.posInt([], 2))
    assert.equal(false, is.posInt("", 2))
    assert.equal(false, is.posInt("   ", 2))
    assert.equal(false, is.posInt("asdf", 2))
    assert.equal(false, is.posInt("1.23", 2))
    assert.equal(false, is.posInt("-42", 2))
    assert.equal(false, is.posInt("-42.01", 2))
    assert.equal(false, is.posInt("-1", 2))
    assert.equal(false, is.posInt("0", 2))
    assert.equal(false, is.posInt("666", 2))
    assert.equal(false, is.posInt(1.23, 2))
    assert.equal(false, is.posInt(-42, 2))
    assert.equal(false, is.posInt(-1, 2))
    assert.equal(false, is.posInt(0, 2))
    assert.equal(false, is.posInt(1, 2))
    assert.equal(true,  is.posInt(666, 2))

    assert.equal(false, is.posInt(fn, -2))
    assert.equal(false, is.posInt(new Date(), -2))
    assert.equal(false, is.posInt(undefined, -2))
    assert.equal(false, is.posInt(NaN, -2))
    assert.equal(false, is.posInt(null, -2))
    assert.equal(false, is.posInt(true, -2))
    assert.equal(false, is.posInt(false, -2))
    assert.equal(false, is.posInt({}, -2))
    assert.equal(false, is.posInt([], -2))
    assert.equal(false, is.posInt("", -2))
    assert.equal(false, is.posInt("   ", -2))
    assert.equal(false, is.posInt("asdf", -2))
    assert.equal(false, is.posInt("1.23", -2))
    assert.equal(false, is.posInt("-42", -2))
    assert.equal(false, is.posInt("-42.01", -2))
    assert.equal(false, is.posInt("-1", -2))
    assert.equal(false, is.posInt("0", -2))
    assert.equal(false, is.posInt("666", -2))
    assert.equal(false, is.posInt(1.23, -2))
    assert.equal(false, is.posInt(-42, -2))
    assert.equal(false, is.posInt(-1, -2))
    assert.equal(false, is.posInt(0, -2))
    assert.equal(true,  is.posInt(1, -2))
    assert.equal(true,  is.posInt(666, -2))
  })

  it('not.posInt + arg', function(){
    assert.equal(true , is.not.posInt(fn, 2))
    assert.equal(true , is.not.posInt(new Date(), 2))
    assert.equal(true , is.not.posInt(undefined, 2))
    assert.equal(true , is.not.posInt(NaN, 2))
    assert.equal(true , is.not.posInt(null, 2))
    assert.equal(true , is.not.posInt(true, 2))
    assert.equal(true , is.not.posInt(false, 2))
    assert.equal(true , is.not.posInt({}, 2))
    assert.equal(true , is.not.posInt([], 2))
    assert.equal(true , is.not.posInt("", 2))
    assert.equal(true , is.not.posInt("   ", 2))
    assert.equal(true , is.not.posInt("asdf", 2))
    assert.equal(true , is.not.posInt("1.23", 2))
    assert.equal(true , is.not.posInt("-42", 2))
    assert.equal(true , is.not.posInt("-42.01", 2))
    assert.equal(true , is.not.posInt("-1", 2))
    assert.equal(true , is.not.posInt("0", 2))
    assert.equal(true , is.not.posInt("666", 2))
    assert.equal(true , is.not.posInt(1.23, 2))
    assert.equal(true , is.not.posInt(-42, 2))
    assert.equal(true , is.not.posInt(-1, 2))
    assert.equal(true , is.not.posInt(0, 2))
    assert.equal(true , is.not.posInt(1, 2))
    assert.equal(false, is.not.posInt(666, 2))

    assert.equal(true , is.not.posInt(fn, -2))
    assert.equal(true , is.not.posInt(new Date(), -2))
    assert.equal(true , is.not.posInt(undefined, -2))
    assert.equal(true , is.not.posInt(NaN, -2))
    assert.equal(true , is.not.posInt(null, -2))
    assert.equal(true , is.not.posInt(true, -2))
    assert.equal(true , is.not.posInt(false, -2))
    assert.equal(true , is.not.posInt({}, -2))
    assert.equal(true , is.not.posInt([], -2))
    assert.equal(true , is.not.posInt("", -2))
    assert.equal(true , is.not.posInt("   ", -2))
    assert.equal(true , is.not.posInt("asdf", -2))
    assert.equal(true , is.not.posInt("1.23", -2))
    assert.equal(true , is.not.posInt("-42", -2))
    assert.equal(true , is.not.posInt("-42.01", -2))
    assert.equal(true , is.not.posInt("-1", -2))
    assert.equal(true , is.not.posInt("0", -2))
    assert.equal(true , is.not.posInt("666", -2))
    assert.equal(true , is.not.posInt(1.23, -2))
    assert.equal(true , is.not.posInt(-42, -2))
    assert.equal(true , is.not.posInt(-1, -2))
    assert.equal(true , is.not.posInt(0, -2))
    assert.equal(false, is.not.posInt(1, -2))
    assert.equal(false, is.not.posInt(666, -2))
  })

  it('aPositiveInteger + arg', function(){
    assert.equal(false, is.aPositiveInteger(fn, 2))
    assert.equal(false, is.aPositiveInteger(new Date(), 2))
    assert.equal(false, is.aPositiveInteger(undefined, 2))
    assert.equal(false, is.aPositiveInteger(NaN, 2))
    assert.equal(false, is.aPositiveInteger(null, 2))
    assert.equal(false, is.aPositiveInteger(true, 2))
    assert.equal(false, is.aPositiveInteger(false, 2))
    assert.equal(false, is.aPositiveInteger({}, 2))
    assert.equal(false, is.aPositiveInteger([], 2))
    assert.equal(false, is.aPositiveInteger("", 2))
    assert.equal(false, is.aPositiveInteger("   ", 2))
    assert.equal(false, is.aPositiveInteger("asdf", 2))
    assert.equal(false, is.aPositiveInteger("1.23", 2))
    assert.equal(false, is.aPositiveInteger("-42", 2))
    assert.equal(false, is.aPositiveInteger("-42.01", 2))
    assert.equal(false, is.aPositiveInteger("-1", 2))
    assert.equal(false, is.aPositiveInteger("0", 2))
    assert.equal(true , is.aPositiveInteger("666", 2))
    assert.equal(false, is.aPositiveInteger(1.23, 2))
    assert.equal(false, is.aPositiveInteger(-42, 2))
    assert.equal(false, is.aPositiveInteger(-1, 2))
    assert.equal(false, is.aPositiveInteger(0, 2))
    assert.equal(false, is.aPositiveInteger(1, 2))
    assert.equal(true , is.aPositiveInteger(666, 2))

    assert.equal(false, is.aPositiveInteger(fn, -2))
    assert.equal(false, is.aPositiveInteger(new Date(), -2))
    assert.equal(false, is.aPositiveInteger(undefined, -2))
    assert.equal(false, is.aPositiveInteger(NaN, -2))
    assert.equal(false, is.aPositiveInteger(null, -2))
    assert.equal(false, is.aPositiveInteger(true, -2))
    assert.equal(false, is.aPositiveInteger(false, -2))
    assert.equal(false, is.aPositiveInteger({}, -2))
    assert.equal(false, is.aPositiveInteger([], -2))
    assert.equal(false, is.aPositiveInteger("", -2))
    assert.equal(false, is.aPositiveInteger("   ", -2))
    assert.equal(false, is.aPositiveInteger("asdf", -2))
    assert.equal(false, is.aPositiveInteger("1.23", -2))
    assert.equal(false, is.aPositiveInteger("-42", -2))
    assert.equal(false, is.aPositiveInteger("-42.01", -2))
    assert.equal(false, is.aPositiveInteger("-1", -2))
    assert.equal(false, is.aPositiveInteger("0", -2))
    assert.equal(true , is.aPositiveInteger("666", -2))
    assert.equal(false, is.aPositiveInteger(1.23, -2))
    assert.equal(false, is.aPositiveInteger(-42, -2))
    assert.equal(false, is.aPositiveInteger(-1, -2))
    assert.equal(false, is.aPositiveInteger(0, -2))
    assert.equal(true , is.aPositiveInteger(1, -2))
    assert.equal(true , is.aPositiveInteger(666, -2))
  })

  it('not.aPositiveInteger + arg', function(){
    assert.equal(true , is.not.aPositiveInteger(fn, 2))
    assert.equal(true , is.not.aPositiveInteger(new Date(), 2))
    assert.equal(true , is.not.aPositiveInteger(undefined, 2))
    assert.equal(true , is.not.aPositiveInteger(NaN, 2))
    assert.equal(true , is.not.aPositiveInteger(null, 2))
    assert.equal(true , is.not.aPositiveInteger(true, 2))
    assert.equal(true , is.not.aPositiveInteger(false, 2))
    assert.equal(true , is.not.aPositiveInteger({}, 2))
    assert.equal(true , is.not.aPositiveInteger([], 2))
    assert.equal(true , is.not.aPositiveInteger("", 2))
    assert.equal(true , is.not.aPositiveInteger("   ", 2))
    assert.equal(true , is.not.aPositiveInteger("asdf", 2))
    assert.equal(true , is.not.aPositiveInteger("1.23", 2))
    assert.equal(true , is.not.aPositiveInteger("-42", 2))
    assert.equal(true , is.not.aPositiveInteger("-42.01", 2))
    assert.equal(true , is.not.aPositiveInteger("-1", 2))
    assert.equal(true , is.not.aPositiveInteger("0", 2))
    assert.equal(false, is.not.aPositiveInteger("666", 2))
    assert.equal(true , is.not.aPositiveInteger(1.23, 2))
    assert.equal(true , is.not.aPositiveInteger(-42, 2))
    assert.equal(true , is.not.aPositiveInteger(-1, 2))
    assert.equal(true , is.not.aPositiveInteger(0, 2))
    assert.equal(true , is.not.aPositiveInteger(1, 2))
    assert.equal(false, is.not.aPositiveInteger(666, 2))

    assert.equal(true , is.not.aPositiveInteger(fn, -2))
    assert.equal(true , is.not.aPositiveInteger(new Date(), -2))
    assert.equal(true , is.not.aPositiveInteger(undefined, -2))
    assert.equal(true , is.not.aPositiveInteger(NaN, -2))
    assert.equal(true , is.not.aPositiveInteger(null, -2))
    assert.equal(true , is.not.aPositiveInteger(true, -2))
    assert.equal(true , is.not.aPositiveInteger(false, -2))
    assert.equal(true , is.not.aPositiveInteger({}, -2))
    assert.equal(true , is.not.aPositiveInteger([], -2))
    assert.equal(true , is.not.aPositiveInteger("", -2))
    assert.equal(true , is.not.aPositiveInteger("   ", -2))
    assert.equal(true , is.not.aPositiveInteger("asdf", -2))
    assert.equal(true , is.not.aPositiveInteger("1.23", -2))
    assert.equal(true , is.not.aPositiveInteger("-42", -2))
    assert.equal(true , is.not.aPositiveInteger("-42.01", -2))
    assert.equal(true , is.not.aPositiveInteger("-1", -2))
    assert.equal(true , is.not.aPositiveInteger("0", -2))
    assert.equal(false, is.not.aPositiveInteger("666", -2))
    assert.equal(true , is.not.aPositiveInteger(1.23, -2))
    assert.equal(true , is.not.aPositiveInteger(-42, -2))
    assert.equal(true , is.not.aPositiveInteger(-1, -2))
    assert.equal(true , is.not.aPositiveInteger(0, -2))
    assert.equal(false, is.not.aPositiveInteger(1, -2))
    assert.equal(false, is.not.aPositiveInteger(666, -2))
  })

  it('aPosInt + arg', function(){
    assert.equal(false, is.aPosInt(fn, 2))
    assert.equal(false, is.aPosInt(new Date(), 2))
    assert.equal(false, is.aPosInt(undefined, 2))
    assert.equal(false, is.aPosInt(NaN, 2))
    assert.equal(false, is.aPosInt(null, 2))
    assert.equal(false, is.aPosInt(true, 2))
    assert.equal(false, is.aPosInt(false, 2))
    assert.equal(false, is.aPosInt({}, 2))
    assert.equal(false, is.aPosInt([], 2))
    assert.equal(false, is.aPosInt("", 2))
    assert.equal(false, is.aPosInt("   ", 2))
    assert.equal(false, is.aPosInt("asdf", 2))
    assert.equal(false, is.aPosInt("1.23", 2))
    assert.equal(false, is.aPosInt("-42", 2))
    assert.equal(false, is.aPosInt("-42.01", 2))
    assert.equal(false, is.aPosInt("-1", 2))
    assert.equal(false, is.aPosInt("0", 2))
    assert.equal(true , is.aPosInt("666", 2))
    assert.equal(false, is.aPosInt(1.23, 2))
    assert.equal(false, is.aPosInt(-42, 2))
    assert.equal(false, is.aPosInt(-1, 2))
    assert.equal(false, is.aPosInt(0, 2))
    assert.equal(false, is.aPosInt(1, 2))
    assert.equal(true , is.aPosInt(666, 2))

    assert.equal(false, is.aPosInt(fn, -2))
    assert.equal(false, is.aPosInt(new Date(), -2))
    assert.equal(false, is.aPosInt(undefined, -2))
    assert.equal(false, is.aPosInt(NaN, -2))
    assert.equal(false, is.aPosInt(null, -2))
    assert.equal(false, is.aPosInt(true, -2))
    assert.equal(false, is.aPosInt(false, -2))
    assert.equal(false, is.aPosInt({}, -2))
    assert.equal(false, is.aPosInt([], -2))
    assert.equal(false, is.aPosInt("", -2))
    assert.equal(false, is.aPosInt("   ", -2))
    assert.equal(false, is.aPosInt("asdf", -2))
    assert.equal(false, is.aPosInt("1.23", -2))
    assert.equal(false, is.aPosInt("-42", -2))
    assert.equal(false, is.aPosInt("-42.01", -2))
    assert.equal(false, is.aPosInt("-1", -2))
    assert.equal(false, is.aPosInt("0", -2))
    assert.equal(true , is.aPosInt("666", -2))
    assert.equal(false, is.aPosInt(1.23, -2))
    assert.equal(false, is.aPosInt(-42, -2))
    assert.equal(false, is.aPosInt(-1, -2))
    assert.equal(false, is.aPosInt(0, -2))
    assert.equal(true , is.aPosInt(1, -2))
    assert.equal(true , is.aPosInt(666, -2))
  })

  it('not.aPositiveInteger + arg', function(){
    assert.equal(true , is.not.aPosInt(fn, 2))
    assert.equal(true , is.not.aPosInt(new Date(), 2))
    assert.equal(true , is.not.aPosInt(undefined, 2))
    assert.equal(true , is.not.aPosInt(NaN, 2))
    assert.equal(true , is.not.aPosInt(null, 2))
    assert.equal(true , is.not.aPosInt(true, 2))
    assert.equal(true , is.not.aPosInt(false, 2))
    assert.equal(true , is.not.aPosInt({}, 2))
    assert.equal(true , is.not.aPosInt([], 2))
    assert.equal(true , is.not.aPosInt("", 2))
    assert.equal(true , is.not.aPosInt("   ", 2))
    assert.equal(true , is.not.aPosInt("asdf", 2))
    assert.equal(true , is.not.aPosInt("1.23", 2))
    assert.equal(true , is.not.aPosInt("-42", 2))
    assert.equal(true , is.not.aPosInt("-42.01", 2))
    assert.equal(true , is.not.aPosInt("-1", 2))
    assert.equal(true , is.not.aPosInt("0", 2))
    assert.equal(false, is.not.aPosInt("666", 2))
    assert.equal(true , is.not.aPosInt(1.23, 2))
    assert.equal(true , is.not.aPosInt(-42, 2))
    assert.equal(true , is.not.aPosInt(-1, 2))
    assert.equal(true , is.not.aPosInt(0, 2))
    assert.equal(true , is.not.aPosInt(1, 2))
    assert.equal(false, is.not.aPosInt(666, 2))

    assert.equal(true , is.not.aPosInt(fn, -2))
    assert.equal(true , is.not.aPosInt(new Date(), -2))
    assert.equal(true , is.not.aPosInt(undefined, -2))
    assert.equal(true , is.not.aPosInt(NaN, -2))
    assert.equal(true , is.not.aPosInt(null, -2))
    assert.equal(true , is.not.aPosInt(true, -2))
    assert.equal(true , is.not.aPosInt(false, -2))
    assert.equal(true , is.not.aPosInt({}, -2))
    assert.equal(true , is.not.aPosInt([], -2))
    assert.equal(true , is.not.aPosInt("", -2))
    assert.equal(true , is.not.aPosInt("   ", -2))
    assert.equal(true , is.not.aPosInt("asdf", -2))
    assert.equal(true , is.not.aPosInt("1.23", -2))
    assert.equal(true , is.not.aPosInt("-42", -2))
    assert.equal(true , is.not.aPosInt("-42.01", -2))
    assert.equal(true , is.not.aPosInt("-1", -2))
    assert.equal(true , is.not.aPosInt("0", -2))
    assert.equal(false, is.not.aPosInt("666", -2))
    assert.equal(true , is.not.aPosInt(1.23, -2))
    assert.equal(true , is.not.aPosInt(-42, -2))
    assert.equal(true , is.not.aPosInt(-1, -2))
    assert.equal(true , is.not.aPosInt(0, -2))
    assert.equal(false, is.not.aPosInt(1, -2))
    assert.equal(false, is.not.aPosInt(666, -2))
  })

})
